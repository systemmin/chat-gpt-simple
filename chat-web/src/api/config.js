const BASE_URL = 'http://localhost:8080';

const chatUrl =`${BASE_URL}/chat/token`;
const eventsUrl =`${BASE_URL}/events`;
const modelsUrl =`${BASE_URL}/models`;

module.exports = {
	chatUrl,
	eventsUrl,
	modelsUrl,
}
